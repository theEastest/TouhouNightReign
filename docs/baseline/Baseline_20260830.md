# CURRENT_TECH_BASELINE

日期：2026-08-30  
用途：新联机任务书 1/3 的可回滚基线记录。本文档只记录现状，不代表所有文件已提交。

## Git

- 仓库：`E:\gemesmods\stg\TouHouNightReign`
- HEAD：`c85a88d fix: align menu and map UI text`
- 工作区：非干净状态；既有跟踪修改和大量未跟踪 Legacy 资源并存。
- 基线策略：先保存本文档和 `BASELINE_MANIFEST.md`，不自动把第三方原始资源全部加入同一提交。

近期提交：

```text
c85a88d fix: align menu and map UI text
eb2312c fix: render map geometry with loaded white texture
414089f fix: generate non-crossing left-to-right map graph
659dc1c feat: add menu and interactive map navigation
10cc93b feat: redesign full exploration map UI
7338297 docs: correct project launch path
6ca85bd feat: add playable fallback STG encounter runtime
f926729 Merge remote-tracking branch 'origin/main'
9baf294 Initial commit
```

## Engine and launch

- LuaSTG Sub：`E:\gemesmods\stg\LuaSTG-Sub-master\build\amd64\bin\LuaSTGSub.exe`
- CMake：`D:\cmake\CMAKE\bin\cmake.exe`，版本 3.31.12
- 引擎工作目录：`E:\gemesmods\stg\TouHouNightReign\game`
- 默认窗口：1280x720，窗口化，60 FPS，VSync 开启。

```powershell
Set-Location 'E:\gemesmods\stg\TouHouNightReign\game'
& 'E:\gemesmods\stg\LuaSTG-Sub-master\build\amd64\bin\LuaSTGSub.exe'
```

## LAN 启动

Host：

```powershell
$env:TNR_NETWORK_MODE='host'
$env:TNR_PLAYER_ID='1'
$env:TNR_HOST='0.0.0.0'
$env:TNR_PORT='27123'
$env:TNR_RUN_SEED='20260826'
Set-Location 'E:\gemesmods\stg\TouHouNightReign\game'
& 'E:\gemesmods\stg\LuaSTG-Sub-master\build\amd64\bin\LuaSTGSub.exe'
```

Client 使用 `TNR_NETWORK_MODE='client'`、`TNR_PLAYER_ID='2'`，并将 `TNR_HOST` 设置为 Host 地址；默认端口为 `27123`。开发空房间检查使用 `TNR_EMPTY_ROOM=1`，原生房间冒烟检查使用 `TNR_NATIVE_AUTO_ROOM=ENEMY|ELITE|BOSS`。

## 运行时边界

- TNR 主入口：`game/scripts/main.lua`。
- 状态机：`game/scripts/tnr/bootstrap.lua`。
- LAN：`game/scripts/tnr/multiplayer/lan_transport.lua`，LuaSocket TCP 长度帧。
- 原生桥：`game/scripts/legacy_native_main.lua` 与 `TNRNativeLegacy`。
- 原生资源/脚本：`game/legacy/`、`game/scripts/LegacyTHlib/`、`game/scripts/THlib.lua`。
- TNR 角色/装备数据：`game/scripts/tnr/character/`、`equipment/`、`preparation/`。

当前仍是混合模型：原生 Legacy runtime 使用 `lstg.var` 和全局 `player`，TNR 使用 `PlayerState`/`PartyState。本任务书将增加 Adapter 和诊断，但不会提前接管 Weapon/Support Runtime。

## 资源规模快照

- `game/legacy/`：约 1,580 个文件。
- `game/assets/`：约 210 个文件。
- 资源清单：`game/assets/reference/legacy_resources.json`、`game/scripts/tnr/stages/legacy_resources.lua`。
- 目录用途见 `BASELINE_MANIFEST.md`。

## Run Seed 与验证

`TNR_RUN_SEED` 由 `main.lua` 读取并传给 `Bootstrap:start_game()` / `GameSession:start_new()`；地图节点另存 `content_seed`，原生房间由 encounter seed 启动。

```text
命令：..\LuaSTG-Sub-master\tool\lua\lua54.exe tests\run.lua
结果：TouHouNightReign core tests passed
```

该结果只覆盖纯 Lua 测试，不等于实体 LuaSTG 原生房间和双实例 LAN 已通过。`game/engine.log` 的历史资源、Shader 和工作目录问题见 `PROJECT_CURRENT_STATE_REPORT.md`。

## 回滚说明

后续变更应使用独立提交。不得使用 `git reset --hard` 清理当前混合工作区；如需恢复，应根据提交和清单逐项处理。
